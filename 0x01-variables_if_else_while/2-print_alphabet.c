#include <stdio.h>
/**
 * main - Entry poin
 *
 * Description: Print alphabets in small letters
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int ch;

	for (ch == a; ch <= z; ch++)
	{
		putchar(ch);
		putchar("\n");
		getch();
	}

	return (0);
}
